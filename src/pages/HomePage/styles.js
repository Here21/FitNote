const styles = theme => {
  return {
    container: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      height: '100%'
    },
    calendar: {
      width: 900
    }
  };
};

export default styles;
